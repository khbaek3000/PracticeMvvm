<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/tools">
    <data>
        <variable
            name="dbinVM"
            type="com.example.brianbaek.practicemvvm.dbpractice.UserInsertVM"/>
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <include
            android:id="@+id/dbinsert_inc_toolbar"
            layout="@layout/back_toolbar"/>
        <include
            android:id="@+id/dbinsert_inc_content"
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            layout="@layout/dbinsert_content"
            android:layout_marginTop="30dp"
            app:layout_constraintTop_toBottomOf="@+id/dbinsert_inc_toolbar"/>
        <Button
            android:id="@+id/bt_dbinsert"
            android:layout_width="120dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="@string/insert"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/dbinsert_inc_content"/>
        <Button
            android:id="@+id/bt_dbshow"
            android:layout_width="120dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="@string/select"
            app:layout_constraintTop_toBottomOf="@+id/bt_dbinsert"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            />
    </android.support.constraint.ConstraintLayout>
</layout>